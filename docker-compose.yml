services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ativotrack
      POSTGRES_PASSWORD: ativotrack
      POSTGRES_DB: ativotrack
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: